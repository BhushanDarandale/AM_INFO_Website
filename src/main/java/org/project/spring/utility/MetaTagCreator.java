package org.project.spring.utility;

public class MetaTagCreator {/*

	public static Metatag createProfileMeta(HttpServletRequest request, User user, int size) {
		// Add Metatag
		Metatag metatag = new Metatag();
		metatag.setDatetime(new Date());
		metatag.setDescription(
				"View the profiles of people named " + user.getName() + "." + " Join here to connect with "
						+ user.getName() + " and others you may know." + "See all articles of " + user.getName());
		metatag.setImage("");
		metatag.setLink(request.getRequestURL().toString());
		metatag.setStatus(1);
		metatag.setTitle("Profiles of " + user.getName() + " | Added " + size + " articles");
		metatag.setKeywords(user.getName() + " articles, " + user.getName()
				+ "Dashboard, Java Articles, Arun Maharana Blog, Home page of ARUNMAHARANA.com");
		return metatag;

	}

	public static Metatag createHomePageMeta(HttpServletRequest request) {
		// Add Metatag
		Metatag metatag = new Metatag();
		metatag.setDatetime(new Date());
		metatag.setDescription("Hi, This is Arun maharnaa a Clean Coder. Started bloging ... "
				+ "Here you can find 100+ articles of hot topics and Java articles also. "
				+ "I think this is the best way to prove myself as good as possible. "
				+ "Keep reading my articles, comment and suggest for better." + " thanks for reading. Have Fun.");
		metatag.setImage("");
		metatag.setLink(request.getRequestURL().toString());
		metatag.setStatus(1);
		metatag.setTitle("Arun Maharana's Blog | Home | Java Developer");
		metatag.setKeywords(
				"Arun Maharana, JavaByArun, Java Articles, Arun Maharana Blog, Home page of ARUNMAHARANA.com");
		return metatag;
	}

	public static Metatag createDashboardPageMeta(HttpServletRequest request, User user, int size) {
		// Add Metatag
		Metatag metatag = new Metatag();
		metatag.setDatetime(new Date());
		metatag.setDescription("Dashboard of " + user.getName()
				+ " contains articles statastics. more options for articles like edit, update, etc.");
		metatag.setImage("");
		metatag.setLink(request.getRequestURL().toString());
		metatag.setStatus(1);
		metatag.setTitle("Dashboard of " + user.getName() + " | Added " + size + " articles");
		metatag.setKeywords(user.getName() + " articles, " + user.getName()
				+ "Dashboard, Java Articles, Arun Maharana Blog, Home page of ARUNMAHARANA.com");
		return metatag;
	}

*/}
